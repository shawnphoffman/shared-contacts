# =============================================================================
# Shared Contacts UI - Environment Variables for Local Development
# =============================================================================
# Copy this file to .env.local and update the values for your local setup
# cp .env.example .env.local
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL Database Configuration
# -----------------------------------------------------------------------------
# Database connection string
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
#
# For local PostgreSQL:
# DATABASE_URL=postgresql://postgres:password@localhost:5432/sharedcontacts
#
# For PostgreSQL running in Docker (default docker-compose setup):
DATABASE_URL=postgresql://sharedcontacts:sharedcontacts@localhost:5432/sharedcontacts
#
# For remote PostgreSQL:
# DATABASE_URL=postgresql://user:password@remote-host:5432/database
# DATABASE_URL=postgresql://sharedcontacts:sharedcontacts@localhost:5432/sharedcontacts

# -----------------------------------------------------------------------------
# Node Environment
# -----------------------------------------------------------------------------
# Set to 'development' for local dev, 'production' for production builds
# NODE_ENV=development
