# =============================================================================
# Shared Contacts - Environment Variables Configuration
# =============================================================================
# Copy this file to .env and update the values for your deployment
# cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL Database Configuration
# -----------------------------------------------------------------------------
# Database user for PostgreSQL
POSTGRES_USER=sharedcontacts

# Database password for PostgreSQL
# IMPORTANT: Change this to a strong password in production!
POSTGRES_PASSWORD=sharedcontacts

# Database name
POSTGRES_DB=sharedcontacts

# External port for PostgreSQL (optional, defaults to 5432)
# Set to empty string or remove to use default
POSTGRES_PORT=5432

# -----------------------------------------------------------------------------
# Radicale CardDAV Server Configuration
# -----------------------------------------------------------------------------
# External port for Radicale CardDAV server (optional, defaults to 5232)
# CardDAV clients will connect to this port
RADICALE_PORT=5232

# -----------------------------------------------------------------------------
# Sync Service Configuration
# -----------------------------------------------------------------------------
# Interval in milliseconds for syncing database changes to Radicale
# Default: 5000 (5 seconds)
# Lower values = more frequent syncs but higher CPU usage
SYNC_INTERVAL=5000

# -----------------------------------------------------------------------------
# Management UI Configuration
# -----------------------------------------------------------------------------
# External port for the web management UI (optional, defaults to 3000)
UI_PORT=3000

# Secret key for Better Auth session encryption
# IMPORTANT: Generate a secure random string for production!
# You can generate one with: openssl rand -base64 32
BETTER_AUTH_SECRET=change-me-in-production-generate-a-secure-random-string

# Base URL for Better Auth (used for callbacks and redirects)
# For production, use your actual domain: https://yourdomain.com
BETTER_AUTH_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Database Connection URL (Auto-constructed)
# -----------------------------------------------------------------------------
# The DATABASE_URL is automatically constructed from POSTGRES_* variables above
# If you need to override it, uncomment and set manually:
# DATABASE_URL=postgresql://user:password@postgres:5432/dbname

# -----------------------------------------------------------------------------
# Optional: Development/Testing Overrides
# -----------------------------------------------------------------------------
# Uncomment for development mode (if supported)
# NODE_ENV=development

# -----------------------------------------------------------------------------
# Notes
# -----------------------------------------------------------------------------
# 1. All passwords should be strong and unique in production
# 2. BETTER_AUTH_SECRET must be a secure random string
# 3. BETTER_AUTH_URL should match your actual domain in production
# 4. Ports can be customized if they conflict with other services
# 5. The sync interval can be adjusted based on your needs
